/**
 * Map-specific fallback values and spawn defaults.
 */

export const DEFAULT_SPAWN_HEIGHT = 200;
export const FALLBACK_SPAWN_HEIGHT = 300;
export const DEFAULT_FINISH_LINE_OFFSET = 200;
export const FINISH_LINE_BUFFER = 100;
